package com.example.MusicBlog.REPOSITORY;

public interface UserRepository {

//    @Query("SELECT c FROM Users c WHERE c.userName LIKE CONCAT ('%' , :query, '%')")
//    List<Users> searchUsers(String query);
}
